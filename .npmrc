# Security: audit dependencies on install
audit=true

# Exact versions for reproducibility
save-exact=true

# Note: --ignore-scripts is enforced in CI via workflow.
# Locally, scripts run normally for prisma generate (postinstall).
